export const environment = {
  llmEndpoints: {
    ollama: '/api/mystery/generate', // Example endpoint for Ollama
    openai: '', // Fill in as needed
    groq: '',   // Fill in as needed
    claude: ''  // Fill in as needed
  }
};
